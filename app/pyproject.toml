[project]
name = "app-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.49",
    "kafka-lib",
]

[tool.uv.sources]
kafka-lib = { path = "../kafka_lib" }
